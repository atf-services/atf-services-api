apply plugin: 'java'
apply plugin: 'idea'

jar {
    baseName = 'atf-services-api'
    version =  '1.0-SNAPSHOT'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven { url "http://repo.spring.io/libs-release" }
    maven { url "https://repository.jboss.org/nexus/content/repositories/releases" }
}

dependencies {
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
